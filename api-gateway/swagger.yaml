openapi: 3.0.0
info:
  title: CloudRetail API Gateway
  version: 1.0.0
  description: API Gateway for CloudRetail Microservices
servers:
  - url: http://localhost:3000
    description: Development server
paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        '200':
          description: Service is healthy
  /api-docs:
    get:
      summary: API Documentation
      responses:
        '200':
          description: Swagger UI
