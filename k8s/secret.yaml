apiVersion: v1
kind: Secret
metadata:
  name: cloudretail-secrets
  namespace: cloudretail
  labels:
    app: cloudretail
type: Opaque
data:
  # Base64 encoded values
  # echo -n "postgres" | base64
  DB_USER: cG9zdGdyZXM=
  # echo -n "postgres" | base64
  DB_PASSWORD: cG9zdGdyZXM=
  # Add more secrets as needed for JWT, etc.
